# 胡昱璠 期末答辩稿 — 基坑安全管理系统

> **总时长**：8分钟 | **Part 1**：5min（项目总览 + AI 模块）| **Part 5**：3min（总结）  
> 标记 `[代码位置]` 处为答辩时可提及的文件与行号，斜体为演示操作指引。

---

## Part 1：项目总览 + AI 模块（5min）

---

### 1.1 项目介绍（1min）

各位老师好，我是胡昱璠，我们小组做的是**基坑设备安全监测与智能预警管理系统**。

基坑工程是建筑施工中最危险的环节之一，一旦发生事故后果非常严重。我们的系统面向**监控中心、施工方、维修工程师**三类角色，集成了设备管理、工单调度、AI故障诊断、趋势预测和3D数字孪生。

三个数字快速说明规模：

- **35万条**真实监测数据，覆盖全站仪位移、伺服轴力、钢支撑温度三类传感器；
- **24台**传感器，数据时间跨度从2025年12月到2026年3月，基于PitMonitor-TwinBench学术基准数据集；
- **3角色**闭环管理——admin监控中心管理员、buyer施工方、repairer维修工程师，各有不同的菜单和操作权限。

*[演示操作] 打开首页大屏，展示统计卡片和饼图；快速切换 admin / buyer1 / repairer1 三个账号展示不同菜单。*

---

### 1.2 系统架构（1min）

系统采用前后端分离的三层架构。

前端是 Vue 3 + Element Plus + ECharts + Three.js，负责页面展示和用户交互。前端通过 Axios 自动注入 JWT Token，调用后端的 REST API。

后端是 Spring Boot 3.2 + MyBatis + Spring Security + WebFlux。权限用 JWT 做无状态认证，数据库操作走 MyBatis，AI 调用使用 Spring WebFlux 做异步非阻塞请求。

数据层是 MySQL 8.0 存储 35 万条监测数据，AI 层接入 DeepSeek API。

```
前端: Vue3 + ElementPlus + ECharts + Three.js + Axios
    ↕ REST API + JWT (Bearer Token)
后端: SpringBoot 3.2 + MyBatis + SpringSecurity + WebFlux
    ↕ JDBC / WebClient
数据与AI: MySQL 8.0 (35万条) + DeepSeek AI
```

---

### 1.3 AI 模块演示（2.5min）

接下来是我重点要讲的 AI 模块，这也是我们这个项目和普通增删改查系统最大的区别。

我们基于 DeepSeek API 实现了**三种 AI 模式**，全部集成在 `AiChatController` 中：

> [代码位置] `backend/.../ai/controller/AiChatController.java:29-92`

#### AI 检修助手

第一种模式是**检修助手**。用户在前端选择"AI检修助手"模式，粘贴设备故障描述，AI 会给出故障原因分析、排查步骤和维修方案。

> 前端：`frontend/src/views/AiChat.vue:9` — 模式切换  
> 后端接口：`POST /api/ai/maintenance` → `AiChatController.java:41-51`

核心是 System Prompt，我们定义了一个基坑设备维修专家角色，精通测斜仪、水位计、土压力计、钢筋应力计、锚索测力计、位移计这六类设备。

> [代码位置] `backend/.../ai/service/DeepSeekService.java:23-36` — `SYSTEM_MAINTENANCE`

*[演示操作] 切换到 AI 聊天页面 → 选"AI检修助手" → 粘贴一段故障描述 → 展示 AI 回答。*

#### AI 健康监测

第二种是**健康监测**，这个更有意思。我们把 35 万条数据的统计分析结果直接写进了 System Prompt，让 AI 像一个真正懂基坑工程的专家一样去诊断数据。

具体来说，我们在 Prompt 中注入了伺服轴力计的完整参数表——SP1 的轴力 P95 是 2234kN、4P1 是 1875kN、4P2 是 1518kN，以及每个传感器的力-温相关系数。

> [代码位置] `backend/.../ai/service/DeepSeekService.java:38-94` — `SYSTEM_HEALTH`

然后我们定义了 **9 种异常类型**：2 种物理结构异常——支撑失效和变形加速；7 种传感器与观测异常——瞬时尖峰、长期漂移、数据卡死、数据缺失、时间错位、温度补偿误差、环境传感器故障。

> [代码位置] `backend/.../ai/service/DeepSeekService.java:66-77` — 9 种异常类型定义

这里面有一个很关键的发现：4P1 传感器的力-温相关系数是 0.71，属于强相关。也就是说，如果温度明显变化但 4P1 的轴力没有联动，那就要怀疑传感器卡死了。这比单纯看数据波动要准确得多。

> [代码位置] `backend/.../ai/service/DeepSeekService.java:44-49` — 伺服参数表

*[演示操作] 切换到健康监测模式 → 粘贴一条 SP1 异常数据 → 展示 AI 的异常诊断结果，指出它区分了物理故障和传感器故障。*

#### AI 智能预测

第三种是**智能预测**。在监测数据页面，用户选择传感器和时间范围，点一下 🔮 按钮，系统会从数据库取历史数据、按小时采样、控制数据量在 100 条以内，然后发到 DeepSeek 做趋势预测。

> [代码位置] `backend/.../ai/controller/AiChatController.java:77-92` — `predictAuto` 接口  
> `AiChatController.java:94-132` — `fetchAndFormat` 三类传感器数据获取  
> `AiChatController.java:138-192` — `buildPrompt` 提示词构建（整点采样+量控制）

预测的 System Prompt 设计了一个**四步分析框架**：趋势识别 → 模式匹配 → 推演 → 预警。AI 会输出预测值、变化趋势、风险等级和关键临界点。

> [代码位置] `backend/.../ai/service/DeepSeekService.java:126-159` — `SYSTEM_PREDICT`

*[演示操作] 打开监测数据页 → 选择 SP1 → 点击 🔮 智能预测 → 展示 AI 预测结果。*

#### DeepSeek 连接配置

最后说一下技术配置。连接 DeepSeek API 用的是 Spring WebFlux 的 WebClient，120 秒超时，Bearer Token 认证，API Key 通过环境变量注入。

> [代码位置] `backend/.../ai/config/DeepSeekConfig.java:15-27`  
> `DeepSeekConfig.java:16` — WebClient Bean 定义  
> `DeepSeekConfig.java:18` — `responseTimeout(Duration.ofSeconds(120))`  
> `DeepSeekConfig.java:23` — `defaultHeader("Authorization", "Bearer " + ...)`

---

### 1.4 项目价值（30s）

总结一下我们项目的价值：和普通增删改查系统相比，我们的**数据是真实的**——35万条PitMonitor-TwinBench学术基准数据；**AI 是专业的**——不是简单问答，而是注入工程参数的领域专家系统；**3D 可视化是工程级的**——Three.js 构建的数字孪生，每个传感器带血条健康度、支持点击交互和高度偏移调节，不是仅仅好看的花瓶。

---

## Part 5：总结（3min）

---

### 5.1 项目亮点

回到总结部分，我提炼五个项目亮点：

1. **35万真实数据** — 数据库建表脚本在 `sql/init_schema.sql`，数据来源是 PitMonitor-TwinBench 学术基准数据集，不是生成的假数据。

2. **AI 9种异常诊断** — `DeepSeekService.java:66-77` 定义了完整的异常分类体系，Prompt 中注入了伺服参数表，能区分物理故障和传感器故障。

3. **3D 基坑数字孪生** — 核心文件 `FoundationPit3D.vue`，SketchUp 建模 → GLB 导出 → Three.js 加载，24 台传感器按类型自动分布，CSS2DRenderer 渲染血条。

   > [代码位置] `FoundationPit3D.vue:89-109` — `SENSOR_DEFS` 传感器定义  
   > `FoundationPit3D.vue:185-199` — `computeDefaultPositions()` 自动分布  
   > `FoundationPit3D.vue:324-347` — `createHealthBar()` 血条

4. **工单状态机闭环** — `WorkOrderMapper.xml:66-98` 定义了六步状态流转，每步用 `WHERE status = 前驱状态` 做 SQL 级别并发校验，防止跳跃操作。

   > [代码位置] `backend/.../resources/mapper/WorkOrderMapper.xml:66-98`  
   > 0待派员 → 1待接单 → 2维修中 → 3待验收 → 4已完成

5. **时间序列健康预测** — `sql/train_model.py` 训练脚本，基于 calibration.json 的 P95 阈值做线性回归，力-温相关性交叉验证（偏差 > 0.15 则风险系数 1.5），计算每台设备的剩余安全运行时间。

   > [代码位置] `sql/train_model.py:91-94` — 线性回归拟合  
   > `sql/train_model.py:107-112` — 力温交叉验证  
   > `sql/train_model.py:116-131` — 轴力健康度公式  
   > `backend/.../predict/service/HealthPredictionService.java:21-33` — `@PostConstruct` 加载模型参数

关键数据：4P1 传感器仅 6% 健康度，当前轴力 1499kN，P95=1875，趋势递增，预计 558 小时后触达阈值。

---

### 5.2 快速全流程演示（2min）

现在让我用 admin 账号跑一遍完整的系统流程：

*[打开系统，admin 登录]*

1. **首页大盘** — 14 台设备统计卡片，设备状态饼图，近 7 天告警趋势折线图。数据来自 `DashboardController.java:20-41` 的四个统计接口。

2. **监测数据** — 切到"伺服轴力"Tab，选 SP1，展示时序曲线，拖拽 dataZoom 缩放。这是 `MonitorController.java:31-39` 的轴向力查询接口。

3. **AI 助手** — 切到"健康监测"模式，粘贴一段异常数据，看 AI 如何诊断异常类型和给出处理建议。接口在 `AiChatController.java:53-63`。

4. **基坑模型** — 3D 全景旋转，缩放，点击 4P1 传感器球，查看数据弹窗，看到上方血条显示 6% 红色。核心逻辑在 `FoundationPit3D.vue`。

5. **工单调度** — 创建工单 → admin 派单给 repairer1 → repairer1 签到 → 填维修日志 → 提交 → buyer1 验收通过。整个流程走 `WorkOrderMapper.xml` 的状态机。

> [代码位置] 工单状态机：`backend/.../resources/mapper/WorkOrderMapper.xml:66-98`

---

### 5.3 改进方向

最后说说未来可以改进的方向：

- **WebSocket 实时推送**：当前监测数据是前端定时拉取，未来可以接入 WebSocket 实现服务端主动推送，达到真正的实时告警。
- **集成 LSTM 深度学习**：现在健康预测用的是线性回归，精度有限。引入 LSTM 时序模型可以提升长周期预测准确率。
- **移动端适配**：现场巡检人员用手机操作会比打开电脑方便得多。
- **MQTT 物联网接入**：直接对接传感器网关，实现设备数据的自动采集入库，省去人工导入环节。

我的汇报完毕，感谢各位老师！
